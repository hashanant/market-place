import { Grid } from '@mui/material';
import styled from 'styled-components';

const HeaderField = styled(Grid)(() => ({
  padding: '1.5% 5%',
  paddingBottom: '40px',
  background: '#222831',
}));

export { HeaderField };
