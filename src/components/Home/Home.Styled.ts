import { Grid } from '@mui/material';
import styled from 'styled-components';

const HeaderField = styled(Grid)(() => ({
  padding: '20px 50px',
  paddingBottom: '40px',
  background: 'maroon',
  color: 'moccasin',
}));

export { HeaderField };
