import { Typography } from '@mui/material';

const EmptyCart = () => {
  return (
    <Typography variant='h3'>
      Please add items to cart. No Items to display.
    </Typography>
  );
};

export default EmptyCart;
