import { Grid } from '@mui/material';
import styled from 'styled-components';

const ContentField = styled(Grid)(() => ({
  display: 'flex',
  justifyContent: 'center',
  padding: '3% 10%',
}));

export { ContentField };
