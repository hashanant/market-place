import { Grid } from '@mui/material';
import styled from 'styled-components';

const HeaderField = styled(Grid)(() => ({
  padding: '20px 50px',
  background: 'maroon',
  color: 'white',
}));

export { HeaderField };
